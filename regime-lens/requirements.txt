# Regime Lens — runtime + dev deps (home PC, Python 3.11+)
pandas>=2.0
numpy>=1.24
requests>=2.31
websockets>=12.0

# Regime engine / pricing (used by the already-written regime modules)
scipy>=1.11
statsmodels>=0.14
hmmlearn>=0.3
ruptures>=1.1
pandas-ta>=0.3.14b0

# Data sources (Phase 2/3)
# Cross-asset (Phase 2) pulls Yahoo's chart API directly via requests — no
# yfinance, whose curl_cffi backend fails TLS through some proxies.

# Screen
streamlit>=1.33
streamlit-autorefresh>=1.0   # optional: enables the screen's auto-refresh loop

# Dev
pytest>=8.0
